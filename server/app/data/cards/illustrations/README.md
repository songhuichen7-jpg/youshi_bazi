# Card illustrations（脱敏 — 已剥离）

> 此目录原本包含 ~11MB 的单人卡场景插画 PNG 资源，
> 用于命盘卡片背景。出于仓库体积与版权考虑，公开版未包含。
>
> JSON 元数据（`formations.json`、`types.json`、`subtags.json`、`state_thresholds.json`）保留在父目录，
> 完整资源可面试时演示。
